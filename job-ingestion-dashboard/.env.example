# API base URL for the job ingestion service.
# Remote ops: connect via Tailscale, start ./scripts/tunnel-home-services.sh, then use localhost:
NEXT_PUBLIC_JOB_INGESTION_API_URL=http://127.0.0.1:8000
