DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/jobingestion
LOG_LEVEL=INFO
# Hugging Face token — build-time only (Dockerfile); not needed at runtime when HF_HUB_OFFLINE=1
HF_TOKEN=
