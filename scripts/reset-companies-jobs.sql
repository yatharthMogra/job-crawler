-- Reset job data for selected companies (by board_token).
-- Usage: psql $DATABASE_URL -f scripts/reset-companies-jobs.sql
-- Or pass tokens via the Python reset script.

-- Target tokens are substituted by reset_e2e_companies.py
