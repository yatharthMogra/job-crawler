# Job Ingestion

Greenhouse-focused local ingestion backend built with FastAPI, PostgreSQL, and APScheduler.

Runs on port **8000**. Uses the shared repo-root Python env — see [`../README.md`](../README.md).

```bash
# from repo root
./scripts/dev-job-ingestion.sh
```
